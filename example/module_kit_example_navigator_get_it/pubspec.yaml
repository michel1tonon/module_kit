name: module_kit_example_navigator_get_it
description: Example app for module_kit with Navigator and get_it.
publish_to: none

environment:
  sdk: ">=3.2.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  module_kit:
    path: ../../
  get_it: any

flutter:
  uses-material-design: true
